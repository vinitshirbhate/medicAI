[project]
name = "backend"
version = "0.1.0"
description = "Sundara Command clinical decision-support demo backend"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["fastapi>=0.115,<1.0", "uvicorn[standard]>=0.30,<1.0"]
